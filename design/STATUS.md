# Rwasmtime status

## Scaffold

- [x] Pipe-first R API constructors and composition verbs.
- [x] Honest not-implemented execution/memory stubs.
- [x] WASI, limits, callbacks, AOT, arrays, and REPL protocol objects.
- [x] README.Rmd and README.md examples following the Ropendal generated-README pattern.
- [x] Vignettes for API boundaries and sandbox REPL/webR protocol.
- [x] Tinytest scaffold.
- [x] Rust core scaffold without R dependencies, including validated runtime config/features/compiler choices, validated deny-by-default WASI capability specs, validated resource/callback limits, validated callback imports/policies, callback broker queue/tickets/completions, app/session/job shapes, AOT artifact metadata/compatibility checks, store/linker/instance handles, validated memory spans, validated array transport request/view types, component/WIT interface and dynamic copied-value shapes, and validated REPL protocol/session/result types.
- [x] Async callback contract corrected: normal R users should not call drain loops; future Savvy/R adapter should use Rtinycc-style platform callback servicing hidden behind `wt_await()` and generated/bound wrappers.
- [x] Source package metadata, ignore files, and cleanup rules for generated build artifacts.
- [x] R documentation and NAMESPACE generated from roxygen source, following the Ropendal-style `make rd` workflow.
- [x] Agent guardrails explicitly forbid dot-helper proliferation and speculative C API sprawl.
- [x] Minimal R-free C API exercise: installed header, C implementation stubs, symbol whitelist, R-free boundary scan, and Rtinycc compile/roundtrip checks. The same reviewed C API can now be installed with `RWASMTIME_RUST_BACKEND=1` to build/release a real Rust/Wasmtime runtime through `rwasmtime_runtime_build()` and to perform one-shot no-import core Wasm calls through copied bytes/scalar values with explicit limits via `rwasmtime_runtime_call_core()`.
- [x] R-free Rust core boundary gate for `src/rust/src`, rejecting Savvy/R API/SEXP/ALTREP tokens before Rust tests run.

## Validation

- 2026-06-13: `R CMD INSTALL --preclean .` succeeds after fixing the R byte-size parser for current R syntax.
- 2026-06-13: `make test-fast` passes 93 tinytest checks after adding callback import/policy validation on top of REPL protocol validation.
- 2026-06-13: `make test-rust` first passes the R-free Rust core boundary gate over 11 Rust source files, then passes 45 Rust unit tests after adding callback import/policy validation for ABI/module rules, duplicate imports, fire-and-forget result restrictions, and reentrant max-depth constraints.
- 2026-06-13: `make test-c-api` passes: the C API symbol whitelist permits exactly the reviewed public `rwasmtime_*` symbols, the R-free boundary checker rejects R headers/SEXP/R API tokens in `inst/include/rwasmtime.h` and `src/c_api.c`, Rtinycc compiles the header, installs the package, links the installed `Rwasmtime.so`, and exercises the exported C status/error/runtime-build stub symbols.
- 2026-06-13: `R CMD check --no-manual Rwasmtime_0.0.0.9000.tar.gz` reports OK.
- 2026-06-13: `make check` completes with 2 CRAN-style NOTEs: new submission/dev version plus placeholder GitHub URLs currently 404, and a platform compiler flag reported by R.
- 2026-06-13: after removing unqualified `eval = FALSE`, `make rdm`, `make rd`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` is back to 2 expected NOTEs and no warnings.
- 2026-06-13: added `make test-api-surface`, which checks the AGENTS preferred function list, `R/api.R` definitions, `NAMESPACE` exports, and `man/Rwasmtime-api.Rd` aliases stay aligned; `make test-fast` now runs this gate before install/tinytest.
- 2026-06-13: after AOT metadata work, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added R-free WIT copied-value scaffolding (`WitType`, `WitValue`, records/lists/options/results/variants, mismatch paths, and `ComponentCallRequest`) and documented that Savvy must convert R inputs to owned Rust value trees rather than passing R objects into the core. After this work, `make test-rust`, `make test-fast`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added R-free memory/array ABI validation: typed byte widths, dimension-to-byte-length checks, memory span overflow/bounds checks, and write payload length checks before the backend stubs return not-implemented. After this work, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added WASI capability validation in Rust and R: preopen guest paths must be absolute, env names must be unambiguous, stdio string/file modes require explicit payloads/paths, and inherited stdio/network are tracked as visible ambient authority. After this work, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added Rust/R resource and callback limit validation: non-negative finite scalar checks in R, explicit zero caps distinct from missing, and reentrant callback policies require max depth at least 2. After this work, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: corrected runtime feature semantics: `relaxed_simd_deterministic` now defaults to `FALSE` in R/Rust and is only valid when `relaxed_simd = TRUE`; relaxed SIMD requires SIMD, component-model async requires component model, and Winch requires `opt_level = "none"`. After this work, `make rdm`, `make rd`, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: tightened REPL protocol boundaries in Rust and R: component/callback REPLs require explicit `eval_export`, mock is reserved for scaffold tests and cannot stand in for webR, non-mock `wt_repl_send()` fails without mutating history, and mock results record text without host evaluation. After this work, `make rd`, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added callback validation in Rust and R: component callbacks cannot set core modules, core/core-msgpack callbacks require modules, duplicate callback import keys fail, fire-and-forget callbacks cannot declare results, and reentrant callback policies require max depth at least 2. After this work, `make rd`, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check` pass; `make check` remains at 2 expected NOTEs and no warnings.
- 2026-06-13: added `make test-rust-boundary` and made `make test-rust` depend on it; the gate scans `src/rust/src/*.rs` and Cargo dependencies for R/Savvy/SEXP/ALTREP/R-API boundary violations while intentionally leaving Savvy adapter generation outside the Rust core.
- 2026-06-13: reran `make rd` so `NAMESPACE` and `man/Rwasmtime-api.Rd` are roxygen-generated from `R/api.R` instead of hand-edited.
- 2026-06-13: corrected callback servicing documentation in `README.Rmd`, `README.md`, `vignettes/api-boundaries.Rmd`, roxygen source, and `design/refinement-log.md`: normal code should not call drain loops; the backend should mirror Rtinycc's POSIX input-handler / Windows message-pump machinery.
- 2026-06-13: rewrote `README.Rmd` in the Ropendal style and regenerated `README.md` with `make rdm`; `README.Rmd` and `_pkgdown.yml` remain repository files but are excluded from package builds via `.Rbuildignore`.
- 2026-06-13: removed unqualified `eval = FALSE` usage from `README.Rmd` and vignettes. README scaffold examples now render by executing safe constructors or capturing honest not-implemented errors; shell install commands are shell fences, not skipped R chunks.
- 2026-06-13: removed speculative native C API draft and avoided `.wt_as_*` helper proliferation; concrete handle validation belongs in Rust/Savvy once the backend exists.
- 2026-06-13: added narrow C API scaffold (`inst/include/rwasmtime.h`, `src/c_api.c`) plus `make test-c-api`, which verifies the reviewed symbol whitelist, checks the C API files are R-free, then uses Rtinycc to compile the header and call the installed native status/error/runtime-build stub symbols. The source tarball includes `inst/include/rwasmtime.h` and excludes repo-only `README.Rmd`, `_pkgdown.yml`, and `tools/`. After adding the symbol whitelist, `make test-c-api`, `make test-fast`, and `R CMD build .` pass.
- 2026-06-14: completed `design/completion-audit.md` for the active goal. Final validation ran `make rd`, `make rdm`, `make test-rust`, `make test-fast`, `make test-c-api`, and `make check`; all passed, with `make check` reporting only the 2 expected NOTEs and no warnings/errors.
- 2026-06-14: started the real backend phase by adding an explicit optional Wasmtime 43 backend (`src/rust/src/backend.rs`) and `make test-rust-backend`. The backend builds a real Wasmtime `Engine` with Cranelift/config feature mapping and compiles/instantiates/calls a core Wasm `(i32, i32) -> i32` module. `make test-rust` remains default/R-free-fast and passes 45 tests; `make test-rust-backend` passes the R-free Rust boundary gate and 48 Rust tests including the real Cranelift smoke test. After this backend slice, `make rd`, `make test-fast`, `make test-c-api`, and `make check` also pass; `make check` remains at the 2 expected NOTEs and no warnings/errors.
- 2026-06-14: added explicit optional `wasmtime-wasi` backend support and `make test-rust-wasi`. The feature-gated WASIp1 runner links `wasi_snapshot_preview1`, builds `WasiCtx` from `WasiSpec`, supports copied stdout/stderr capture, string stdin, and explicit preopens without ambient filesystem grants. `make test-rust-wasi` passes the R-free Rust boundary gate and 50 Rust tests, including real stdin/stdout capture and deny-by-default versus explicit preopen execution tests. Default `make test-rust` still passes 45 tests without enabling the heavy WASI feature. After this WASI slice, `make rd`, `make test-fast`, `make test-c-api`, and `make check` also pass; `make check` remains at the 2 expected NOTEs and no warnings/errors.
- 2026-06-14: added an opt-in Rust-backed C API install path: `RWASMTIME_RUST_BACKEND=1` selects `src/Makevars.rust.in`, builds the Rust staticlib with the `c-api` feature, and links it into `Rwasmtime.so`. The public C symbol surface remains exactly the same 7 reviewed functions; `rwasmtime_runtime_build()` now delegates to the real Wasmtime engine builder in that opt-in build. `make test-c-api-rust-backend` passes the symbol/R-free/header gates, installs the Rust-backed package, and uses Rtinycc to verify real runtime build/release plus invalid feature validation. Default `make test-c-api`, `make test-fast`, `make test-rust`, `make rd`, and `make check` still pass.
- 2026-06-14: added the first real R-to-runtime bridge, then moved it behind generated Savvy wrappers instead of patched `.Call` registrations. `src/savvy` is the R/Savvy adapter crate, `src/init.c` and `R/000-wrappers.R` are generated by `savvy-cli update . src/savvy`, the default C-only build links `src/native_stubs.c` fallback implementations for the generated `savvy_*__ffi` symbols, and the opt-in Rust-backed build links `librwasmtime_savvy.a`. `wt_build_runtime()` now returns `backend = "pending"` with `ptr = NULL` in the default build and `backend = "native"` with a `RwasmtimeNativeRuntime` Savvy handle in the opt-in build. `make test-fast` passes 95 tinytest checks in the default build, and `make test-r-runtime-rust-backend` verifies the native Savvy runtime handle from R.
- 2026-06-14: added the first real R `wt_call()` path for opt-in Rust-backed installs, then replaced the initial signature-specific smoke bridge with dynamic signature discovery through Wasmtime `FuncType` in the generated Savvy adapter. `WtPreparedApp |> wt_call()` now executes numeric scalar core module exports through Wasmtime when the runtime is native, without adding one native entry point per Wasm signature and without custom `.Call` registration patches. The public C API remains at the same 7 whitelisted symbols. `cargo test --features c-api` passes 51 Rust tests including generic i32/i64/f64 call coverage, `make test-r-call-rust-backend` returns 42 and 42.5 from real Wasm exports through the Savvy wrapper, and default `make test-fast` remains green with 95 tinytest checks. After this slice, `make test-rust`, `make test-rust-wasi`, `make test-c-api`, `make test-c-api-rust-backend`, and `make check` pass; `make check` remains at the 2 expected NOTEs and no warnings/errors. A grep over `R`, `src`, `tools`, `inst`, `design`, and `AGENTS.md` confirms the removed signature-specific bridge name no longer exists.
- 2026-06-14: broadened the dynamic core-call adapter beyond numeric scalars while keeping the same Wasmtime `FuncType`/`Val` path. The Savvy adapter now supports exact `i64` decimal-string transport, `v128` as raw vectors of length 16, and `NULL` for nullable reference arguments/results; non-null host references remain out of scope. Validated with `cargo check --manifest-path=src/savvy/Cargo.toml` and `make test-r-call-rust-backend`.
- 2026-06-14: added the first real R-level WASIp1 command path. In opt-in Rust-backed builds, `WtPreparedApp` objects with `WtWasi` can execute `wt_call("_start")` through Wasmtime-WASI and receive a `WtWasiResult` with copied stdout/stderr raw/text fields. WASIp1 stdio now covers explicit binary-safe file stdin by preloading host file bytes in the R/Savvy adapter and passing copied stdin bytes to the backend; `stdout = "file"` / `stderr = "file"` are supported for prepared apps and low-level linked instances as post-execution copied outputs. File stdout/stderr copy captured bytes to explicit, distinct host file paths after command execution; payloads that `rawToChar()` cannot represent, such as embedded-NUL output, remain available in `stdout_raw`/`stderr_raw` and produce `NA` text fields. `make test-r-wasi-rust-backend` installs with `RWASMTIME_RUST_BACKEND=1`, builds the Savvy adapter staticlib, runs real WASIp1 string stdin, adapter-preloaded binary-safe file stdin, mixed file/capture, and binary stdout/stderr-file modules, and verifies captured/file outputs. `cargo test --features c-api,wasi` passes 54 Rust tests, `cargo check --manifest-path=src/savvy/Cargo.toml` passes, and default `make test-fast` remains green.
- 2026-06-14: completed the Ropendal/Rzarrs-style Savvy migration for the R bridge. `make rd` now calls `savvy-cli update . src/savvy` and no longer patches generated `src/init.c`; grep confirms `rwasmtime_r_*`, `tools/patch-init-registration.R`, and `src/rwasmtime_internal.h` are gone. The generated Savvy adapter helpers are internal to the namespace, while the user-facing API remains the pipe-first `wt_*` surface. Validation after the migration: `make test-fast`, `make test-r-runtime-rust-backend`, `make test-r-call-rust-backend`, `make test-r-wasi-rust-backend`, `make test-c-api`, `make test-c-api-rust-backend`, `make test-rust-backend`, `cargo test --features c-api,wasi`, `cargo check --manifest-path=src/savvy/Cargo.toml`, and `make check` all pass; `make check` remains at the 2 expected NOTEs and no warnings/errors.
- 2026-06-14: applied the Ropendal webR shim pattern explicitly. Default native no-backend builds now link `src/native_stubs.c`, webR/wasm configure detection selects `src/Makevars.wasm.in` and links `src/wasm_stubs.c`, and opt-in native Rust builds continue to link `librwasmtime_savvy.a`. Configure probes verified `--host=wasm32-unknown-emscripten` selects `wasm_stubs.o`, default native selects `native_stubs.o`, and `RWASMTIME_RUST_BACKEND=1` selects the Savvy staticlib. `make test-fast`, `make check`, and `make test-r-runtime-rust-backend` pass after the split.
- 2026-06-14: rewrote `NEWS.md` in reverse-chronological R-package style with a development section above the initial scaffold section. Added generated authorship/licence-note infrastructure: `tools/update-authors.R`, `make authors`, `inst/AUTHORS`, `inst/LICENCE.note`, and a DESCRIPTION `Copyright` pointer. `tools/update-authors.R` reads locked Cargo metadata for the opt-in Savvy backend dependencies and emits Rsassy/string2path-style notices.
- 2026-06-14: clarified the REPL target shape after comparing against the MicroPython-in-Wasm pattern. `wt_repl()`/`wt_webr_repl()` should model a persistent guest interpreter VM with an explicit eval/stdio/callback protocol and ergonomic R session verbs, not repeated stateless `_start` calls or any host-R evaluation shortcut. The next backend work should prioritize persistent sessions and protocol transport that make this possible.
- 2026-06-14: promoted the Rust `CoreInstance` foundation into R/Savvy persistent handles. `RwasmtimeNativeRuntime$instantiate_core()` now returns a generated Savvy `RwasmtimeNativeInstance`; `wt_new_session()` stores it for native core modules; `wt_call()` on `WtSession` reuses the same instance; and `wt_memory_size()`, `wt_memory_grow()`, `wt_memory_read(dtype = "u8")`, and `wt_memory_write(dtype = "u8")` now operate on that session's exported memory. Validated with `cargo check --manifest-path=src/savvy/Cargo.toml`, `make rd`, `make test-fast`, `make test-r-memory-rust-backend`, `make test-r-call-rust-backend`, `make test-r-wasi-rust-backend`, `make test-rust-backend`, `make test-c-api`, `make test-c-api-rust-backend`, and `make check` (2 expected NOTEs only).
- 2026-06-14: widened the R-side linear-memory copy transport over the same native raw memory boundary. `wt_memory_read()` and `wt_memory_write()` now support `i32`, `u32`, `f32`, `f64`, `i64`, and `u64`; 64-bit integers use exact decimal string transport because base R has no native int64 scalar and doubles silently lose precision above 2^53. Native `wt_memory_grow()`, `wt_memory_write()`, and `wt_array_write()` check configured `WtLimits$memory_bytes` for R-triggered memory operations and fail with classed `rwasmtime_limit_error` conditions when the requested range or current memory size exceeds the limit. `v128` and borrowed views remain explicit future/advanced boundaries. Validated with `make test-r-memory-rust-backend`.
- 2026-06-14: added the first real guest-array copy path over the typed memory primitive. `wt_array_write()` now supports native sessions/instances with a guest `alloc` export, writes a copied typed payload into exported memory, returns a `WtArray` descriptor, `wt_as_array()` copies values back, and `wt_free()` calls a guest `free(ptr, byte_length)` export when present. Validated with `make test-r-memory-rust-backend` using a Wasm `sum_f64` reader over the allocated payload.
- 2026-06-14: added a real opt-in core-memory REPL transport. `wt_repl(protocol = "core")` now creates/reuses a persistent native core session, copies UTF-8 source into exported memory using `alloc_export` or `input_ptr`, calls the guest `eval_export(ptr, len)`, then reads UTF-8 value/stdout/stderr/error fields plus status and completion through explicit guest exports. `inst/tinytest/test-rust-backend-repl.R` validates persistent guest state and structured memory transport with a tiny interpreter-like Wasm module; `make test-r-repl-rust-backend`, `make test-r-memory-rust-backend`, `make test-fast`, `make test-rust`, and `make check` pass after the change (`make check` still has only the expected 2 NOTEs).
- 2026-06-14: reaffirmed the lower-level primitive preference. Component/WIT remains a future typed convenience over explicit primitives, not the foundation. Added real opt-in `wt_exec()` for `WtSession`/`WtPreparedApp`/`WtInstance`: it delegates to `wt_call()`, discards the result, and returns `.x` so persistent side-effecting core calls stay pipeable. `inst/tinytest/test-rust-backend-exec.R` validates side effects through repeated calls and memory reads.
- 2026-06-14: added real opt-in low-level core module artifacts. `WasmtimeRuntime::compile_core()` returns a reusable `CoreModule`; the Savvy adapter exposes generated `RwasmtimeNativeModule`; `wt_compile()` stores that handle in `WtArtifact`; and `wt_instantiate()` creates independent persistent `WtInstance` handles for no-import core modules. `wt_call()`, `wt_exec()`, and memory operations now work on those low-level instances. Validated with Rust test `compiled_core_module_can_instantiate_separate_stateful_instances` and `make test-r-low-level-rust-backend`.
- 2026-06-14: added real opt-in core AOT save/load. `CoreModule::serialize()` wraps `wasmtime::Module::serialize()`, `WasmtimeRuntime::deserialize_core()` wraps metadata-gated `wasmtime::Module::deserialize()`, `RwasmtimeNativeModule$serialize()` and `RwasmtimeNativeRuntime$deserialize_core()` are generated by Savvy, `wt_aot_save()` writes serialized module bytes plus a `.rwasmtime.rds` sidecar, and `wt_aot_load()` validates metadata before deserializing. `make test-r-aot-rust-backend` validates save/load/execute and incompatible metadata failure with `rwasmtime_aot_incompatible`.
- 2026-06-14: added low-level WASIp1 linker support for compiled core artifacts. `CoreInstance` can now hold either a plain store or a WASIp1 store, `CoreModule::instantiate_wasi_p1()` links `wasi_snapshot_preview1` from an explicit `WasiSpec`, the Savvy adapter exposes `RwasmtimeNativeModule$instantiate_wasi_p1()` and `RwasmtimeNativeInstance$wasi_output()`, and `wt_instantiate()` now honors `WtLinker |> wt_link_wasi()`. `wt_call("_start")` on the resulting `WtInstance` returns a copied `WtWasiResult`. Validated with Rust test `compiled_core_module_instantiates_with_wasi_p1_imports` and `make test-r-wasi-rust-backend`.
- 2026-06-14: added `make test-webr`, a non-execution wasm/webR build gate. `tools/check-webr-wasm-gate.R` verifies `./configure --host=wasm32-unknown-emscripten` selects `src/Makevars.wasm.in`, wasm configure takes precedence over `RWASMTIME_RUST_BACKEND=1`, `src/Makevars.wasm.in` links `wasm_stubs.o` instead of native stubs or the Savvy staticlib, and `src/wasm_stubs.c` implements every Savvy-generated `savvy_*__ffi` symbol from `src/rust/api.h` with the webR pending-backend message. This does not claim webR guest execution.
- 2026-06-14: migrated R native-backend behavior checks from ad-hoc `tools/check-r-*.R` scripts into installed tinytests: `inst/tinytest/test-rust-backend-*.R`. These files skip at top level in default no-backend installs. Makefile backend targets install the requested build mode and invoke the package test trigger in `tests/tinytest.R` with a file pattern and non-skip enforcement. `tools/` is now reserved for source/build/environment gates, not R behavior assertions.
- 2026-06-14: added real low-level core callback imports. The R-free Rust backend now exposes generic `CoreHostFunc` imports, while the Savvy adapter owns/preserves R closures and binds `WtLinker |> wt_link_callbacks()` to core Wasm function imports declared with `abi = "core"`. `inst/tinytest/test-rust-backend-callbacks.R` validates scalar and multi-result R callbacks called from Wasm, repeated callback state, duplicate imports of the same callback, prepared-app/session callback paths, missing-import failures, low-level and app-level WASI+callbacks rejection, extra callback validation, and `rwasmtime_callback_error` conditions from callback-backed instances, sessions, prepared apps, and immediate jobs. Synchronous native core callbacks now enforce whole-number `wt_callback_policy(max_calls=)` and best-effort elapsed-time `timeout_ms` checks in the R/Savvy adapter path. Native core traps now rethrow as `rwasmtime_trap` conditions without reclassing missing-export or callback-link setup errors. Fuel and wall-time limits now reach the native store: `wt_limit_fuel()` maps to Wasmtime fuel, `wt_limit_wall_time()` maps to epoch interruption on targets with 64-bit atomics, and resulting execution-limit traps, including start-section failures during instantiation, are rethrown as `rwasmtime_limit_error` for prepared apps, sessions, and low-level instances. The webR/default stub gate now compares generated Savvy FFI prototypes, not only symbol names, so fallback builds catch arity drift. Full background async cancellation remains future work. `wt_call_async()` also now settles immediately to a completed `WtJob` for native calls that can run synchronously. `wt_prepare()` now stores a native compiled core artifact for module apps and apps built from existing or AOT-loaded `WtArtifact` objects, so prepared calls, sessions, and immediate jobs reuse compiled modules. Low-level `wt_instantiate()` now rejects store/linker/runtime mismatches before native instantiation. Validated with `cargo check --manifest-path=src/savvy/Cargo.toml`, `cargo check --manifest-path=src/rust/Cargo.toml --features c-api,wasi`, `cargo test --manifest-path=src/rust/Cargo.toml --features wasmtime compiled_core_module_instantiates_with_host_callback_imports`, `make test-webr`, `make test-fast`, `make test-r-call-rust-backend`, `make test-r-low-level-rust-backend`, `make test-r-aot-rust-backend`, `make test-r-wasi-rust-backend`, `make test-r-memory-rust-backend`, and `make test-r-callbacks-rust-backend`.
- 2026-06-14: extended compact print coverage to remaining public value objects and capability specs: `WtComponentSpec`, `WtArrayArgument`, `WtWasi`, `WtLimits`, `WtCallbacks`, and `WtCallbackPolicy`, and made artifact-backed `WtAppSpec` printing use an artifact label instead of list internals. `WtWasi` now prints stdio modes, file paths, preopen mappings, and ambient authority flags. Pending `wt_await(timeout_ms = ...)` calls now fail with a classed `rwasmtime_timeout` condition while no-timeout pending awaits remain honest `rwasmtime_not_implemented` boundaries. Regenerated roxygen/NAMESPACE with `make rd` and validated the default no-backend behavior with `make test-fast`.
- 2026-06-15: after the binary-safe adapter-preloaded WASI stdin-file slice, the full native backend matrix passed: `make test-r-runtime-rust-backend`, `make test-r-call-rust-backend`, `make test-r-callbacks-rust-backend`, `make test-r-exec-rust-backend`, `make test-r-low-level-rust-backend`, `make test-r-memory-rust-backend`, `make test-r-repl-rust-backend`, `make test-r-aot-rust-backend`, and `make test-r-wasi-rust-backend`.
- 2026-06-15: native `WtLimits` resource caps now cross the generated R/Savvy boundary into Wasmtime stores. `CoreExecutionLimits` carries `memory_bytes`, `table_elements`, and `instances` in addition to fuel/wall time; stores install a `ResourceLimiter` before instantiation; guest-internal `memory.grow` and initial memories above `wt_limit_memory()` now fail before memory mutation and are rethrown as classed `rwasmtime_limit_error` conditions with parsed `limit`/`requested` fields where available. Table element caps are enforced for initial/growing tables. Instance caps are passed to each native store created for a session/instance, but `WtStore` is still an R-side spec object rather than a reusable native store handle, so store-wide instance accounting across repeated `wt_instantiate()` calls remains future work. `wt_with_limits(...)` now forwards memory/table/instance/fuel/wall-time dot arguments or rejects unsupported names, and `wt_limit_tables()` / `wt_limit_instances()` require whole finite non-negative scalars. Validated with targeted Rust store-limiter tests, full `cargo test --manifest-path=src/rust/Cargo.toml --features wasmtime`, `make rd`, `cargo check --manifest-path=src/savvy/Cargo.toml`, `cargo check --manifest-path=src/rust/Cargo.toml --features c-api,wasi`, `make test-webr`, `make test-fast`, and the full native backend matrix.
- 2026-06-15: added the first real native component metadata boundary. Native runtimes can now compile component sources for `wt_component_imports()` / `wt_component_exports()` and return copied `WtComponentItem` metadata with names, kinds, and simple parameter/result schema labels. This deliberately does not instantiate or call components and does not claim full WIT value conversion. Validated with Rust component-introspection tests, native installed `test-component-wit.R`, `cargo check --manifest-path=src/rust/Cargo.toml --features c-api,wasi`, `cargo check --manifest-path=src/savvy/Cargo.toml`, `make test-webr`, `make test-fast`, and `make check`.
- 2026-06-15: extended the Ropendal-style installed C API from a build/release smoke boundary to a tiny real execution boundary. `RWASMTIME_C_API_VERSION` is now 2, `rwasmtime_core_value_t` and `rwasmtime_core_call_options_t` are public, and `rwasmtime_runtime_call_core()` performs a one-shot no-import core module call from copied bytes with copied scalar values and explicit optional resource/fuel/wall-time limits. Default builds export the symbol but fail honestly; Rust-backend builds delegate to Wasmtime through the R-free Rust C ABI. The opt-in Rust link now hides private staticlib shim symbols with `--exclude-libs`, and `tools/check-c-api-rust-backend.R` audits the installed shared library so only the reviewed public `rwasmtime_*` symbols are exported. Validated with the C API symbol whitelist, R-free C boundary scan, header compile, default installed C API roundtrip, Rust-backed installed symbol audit, Rust-backed Rtinycc call of a real Wasm binary returning 42, and Rust FFI unit coverage.
- 2026-06-15: added Ropendal-style project automation for the broad README/site/CI goal. `_pkgdown.yml` now maps the real `wt_*` surface into runtime, capability, execution, artifact, memory/array, component metadata, and REPL groups, and includes the API-boundary and sandbox/webR vignettes as articles. Added GitHub Actions workflows for default-backend tinytests/R CMD check, installed C API contracts, aggregate native Rust backend tinytests, pkgdown Pages deployment, and the webR/wasm stub gate. Added `make test-r-native-rust-backend` so CI can install the native backend once and run all installed native backend tinytests. Validated with YAML parsing, `make test-r-native-rust-backend`, local `pkgdown::build_site(install = FALSE)` with clean pkgdown metadata checks, `make test-fast`, `make test-webr`, and `make check`.

## Backend implementation checklist

- [~] Add `wasmtime`, `wasmtime-wasi`, and `savvy` dependencies deliberately. `wasmtime` and `wasmtime-wasi` are now optional/pinned through explicit Rust backend features for real smoke tests and opt-in C API backend builds; real `savvy` adapter dependencies are still pending.
- [~] Replace R list/env handles with Rust-backed external pointer classes. `wt_build_runtime()` now calls the generated Savvy adapter and returns a `RwasmtimeNativeRuntime` handle when installed with `RWASMTIME_RUST_BACKEND=1`; other handles remain scaffold list/env objects.
- [~] Implement runtime config mapping to Wasmtime `Config`. The feature-gated backend maps compiler strategy, Cranelift optimization level, parallel compilation, component model flags, SIMD/relaxed-SIMD, bulk/multi-memory, memory64, and threads into `wasmtime::Config`; GC remains explicitly rejected until the backend is built with GC support.
- [~] Implement module/component compile and instantiation. The feature-gated backend compiles, instantiates, and calls core modules through a generic dynamic core-value ABI; the opt-in R bridge routes `WtPreparedApp |> wt_call()` for scalar, exact i64-string, v128-raw, and nullable-ref core exports through real Wasmtime. The installed C API exposes only a smaller copied scalar one-shot no-import core-call boundary for downstream native packages. Native component import/export introspection is implemented as copied metadata; component instantiation/calls and full WIT value conversion remain pending.
- [~] Implement WASIp1/WASIp2 linker integration. The explicit `wasi` Rust feature now builds a real WASIp1 linker/context path with copied stdout/stderr capture, copied stdin bytes, explicit preopens, prepared-app command execution, and low-level `WtLinker |> wt_link_wasi()` instantiation for compiled artifacts. Binary-safe host stdin files are preloaded by the R/Savvy adapter; native streaming file descriptors and WASIp2/component WASI integration remain pending.
- [~] Implement callback broker with R-main-thread platform servicing hidden from normal R users. Low-level synchronous core callback imports are implemented for R-main-thread calls through the Savvy adapter; worker-thread broker/wake machinery remains pending.
- [~] Implement async job handles and cancellation with Rtinycc-style cross-platform callback wake/synchronization machinery in the adapter layer. `wt_call_async()` now settles immediately for supported native synchronous calls; true background execution and callback wake/synchronization remain pending.
- [~] Implement AOT serialize/deserialize with compatibility metadata. Core module AOT save/load is implemented for opt-in native artifacts; broader cache policy and component artifacts remain pending.
- [~] Implement memory read/write and typed array transport. Raw plus typed copied linear-memory transport, a guest-allocated `WtArray` copy path, R-triggered memory limit checks, and Wasmtime store-limiter enforcement for initial memory and guest-internal growth are implemented; borrowed views, v128 memory lanes, host arenas, and richer array/tensor convenience remain pending.
- [~] Implement component/WIT dynamic value conversion. Native component import/export metadata introspection is implemented; dynamic component calls and WIT value lowering/lifting remain pending.
- [ ] Implement REPL protocols without host-R eval shortcuts.
